{
  "name": "forma-latex-service",
  "version": "1.0.0",
  "private": true,
  "description": "Self-hosted LuaLaTeX compile microservice for Forma's worksheet and mark-scheme PDFs.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "dev": "tsc -p tsconfig.json --watch"
  },
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
